# Leave blank for same-origin deployments where the backend is served at /api.
VITE_API_BASE_URL=

# Local Vite-only proxy target used by `npm run dev`.
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8000
