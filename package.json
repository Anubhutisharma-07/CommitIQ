{
  "name": "commitiq",
  "version": "1.0.0",
  "private": true,
  "description": "Repository health intelligence for professional engineering teams.",
  "main": "frontend/src/main.tsx",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run test"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
